name: flutter_lapor_book
description: "Proyek Lapor Book."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0-174.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
